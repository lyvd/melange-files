package:
  name: cryptographylib 
  version: 1.2.0
  epoch: 0
  description: "cryptographylib"
  copyright:
    - license: BSD-3-Clause
  dependencies:
    runtime:
      - python3

environment:
  contents:
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    repositories:
      - https://packages.wolfi.dev/os
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - py3-pip
      - py3-setuptools
      - python3
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/lyvd/cryptographylib 
      tag: ${{package.version}}
      expected-commit:  3e3d2e6a47d4af4a7e53207f77ed9c40c5b5b6cd

  - runs: |
      python3 setup.py build 

  - runs: |
      python3 setup.py install --prefix=/usr --root="${{targets.destdir}}" --skip-build

  - uses: strip

update:
  enabled: true
  github:
    identifier: httpie/httpie
    use-tag: true

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        http --version
        httpie --version
        https --version
        http --help
        httpie --help
        https --help
